# nifi-progress-marker-processor
A processor that generates a progress event for different stages a flow. Useful for writing events to a data store to inform external systems where a data point is at.
